# Nguồn dùng để tái tạo dữ liệu
Tải `hsk30.csv` từ `ivankra/hsk30` và `CVDICT.u8` từ `ph0ngp/CVDICT`, đặt vào thư mục này rồi chạy:

```bash
python tools/build_data.py
```

Các file nguồn lớn không được đóng gói lại trong repository này. Dữ liệu JSON đã sinh có ghi nguồn và trạng thái kiểm chứng.
